# <> Release 2.18.x - 

## Functions
- ✅ Fixed `KSA` throwing with `After Effects` versions that do not contain a patch version number (ie. `26.3`)
- 📋 `prem.__getlayerMid()` now uses UIA to determine the middle divider coordinates